This intro will guide you through basic usage of github for your assignment

You'll use these commands to add code to github
	`git add .`  
	`git commit -m "message"`  
	`git push origin HEAD`  

For this first example, your task is to make the test pass by implementing the method `SayHello` in `Greeter.java` class
